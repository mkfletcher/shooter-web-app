export default class PlayerModel {

    constructor (id, name, speed, angle, position) {

        this.id = id,
        this.displayName = displayName,
        this.speed = speed,
        this.angle = angle;
        this.position = position;
    }

}